{
  "name": "auth-driver",
  "version": "2.0.0",
  "description": "A drive to use the auth service",
  "main": "lib/index.js",
  "scripts": {
    "test": "npm run compile && babel-node --presets es2015 tests.js | faucet",
    "compile": "babel --presets es2015 -d lib/ src/",
    "prepublish": "npm run compile"
  },
  "author": "Larlyn Tanasap",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.7.0"
  },
  "devDependencies": {
    "babel-cli": "^6.2.0",
    "babel-preset-es2015": "^6.1.18",
    "bandname": "^1.0.0",
    "blue-tape": "^0.1.11",
    "faucet": "0.0.1",
    "required_env": "^1.0.0"
  }
}
